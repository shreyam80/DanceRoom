export default function DancerPauseCommentModal({comment,onAck}:{comment:any;onAck:()=>void}){if(!comment)return null;return <div className='fixed inset-0 bg-black/50 flex items-center justify-center'><div className='bg-white rounded p-4 w-[420px]'><h3 className='font-bold'>Feedback at {comment.comments?.video_timestamp_seconds?.toFixed?.(1) ?? comment.video_timestamp_seconds}s</h3><p className='my-2'>{comment.comments?.body || comment.body}</p><button className='bg-slate-900 text-white px-3 py-1 rounded' onClick={onAck}>Acknowledge</button></div></div>}
