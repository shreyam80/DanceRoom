import { api } from '../lib/api'; import { useNavigate } from 'react-router-dom';
export default function SignupPage(){const nav=useNavigate(); return <form className='space-y-2 max-w-md bg-white p-4 rounded border' onSubmit={async(e)=>{e.preventDefault();const fd=Object.fromEntries(new FormData(e.currentTarget).entries());await api.post('/auth/signup',fd);nav('/login')}}><h1 className='text-xl font-bold'>Sign up</h1><input name='full_name' placeholder='Full name' className='w-full border p-2'/><input name='username' placeholder='Username' className='w-full border p-2'/><input name='email' placeholder='Email' className='w-full border p-2'/><input name='password' type='password' placeholder='Password' className='w-full border p-2'/><select name='role' className='w-full border p-2'><option value='choreographer'>Choreographer</option><option value='dancer'>Dancer</option></select><button className='bg-slate-900 text-white px-3 py-1 rounded'>Create account</button></form>}
