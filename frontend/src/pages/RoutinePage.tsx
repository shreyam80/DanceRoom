import { useEffect,useState } from 'react'; import { useParams, Link } from 'react-router-dom'; import { api } from '../lib/api';
export default function RoutinePage(){const {routineId}=useParams(); const [data,setData]=useState<any>(); useEffect(()=>{api.get(`/routines/${routineId}`).then(r=>setData(r.data));},[routineId]); if(!data) return <p>Loading...</p>; return <div><h1 className='text-xl font-bold'>{data.routine.title}</h1>{data.videos.map((v:any)=><div key={v.video_id} className='bg-white p-2 border rounded my-2'>V{v.version_number} <Link to={`/videos/${v.video_id}/review`}>Review</Link> | <Link to={`/videos/${v.video_id}/watch`}>Watch</Link></div>)}</div>}
